function showline() {

	var randomNumber = Math.floor(Math.random() * 2);
	var urls = [
		'https://kqotvy.one/ht147',
		'https://kqotvy.one/ht147'
	];
	var selectedUrl = selectedUrl = urls[randomNumber];

	gtag_report_conversion(selectedUrl);
}